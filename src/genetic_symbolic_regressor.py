class GeneticSymbolicRegressor:
    def __init__(self):
        pass

    def optimize(self):
        #initialize population
        #calculate fitness
        #order by fitness
        for epoch in range(epochs):
            print(f"EPOCH={epoch + 1}")
            # get N best and do crossover
            # do mutation
            # calculate fitness
            # order by fitness
            # kill the worst M and create M individuals